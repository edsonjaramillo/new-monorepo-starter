{
  "name": "monorepo-starter",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "turbo build --cache=local:rw,remote:",
    "check": "manypkg check",
    "check-types": "turbo check-types --cache=local:rw,remote:",
    "clean": "turbo clean",
    "clean-cache": "rimraf .turbo/cache && turbo clean-cache",
    "clean-node-modules": "turbo clean-node-modules",
    "dev": "turbo prep && turbo dev --filter=api --filter=admin",
    "docker:dev:down": "docker compose -f ./docker/docker-compose.dev.yml down",
    "docker:dev:up": "docker compose -f ./docker/docker-compose.dev.yml up -d",
    "fix": "manypkg fix",
    "format": "turbo format --cache=local:rw,remote:",
    "graph": "mkdir -p graphs && turbo build --filter=api --graph=graphs/api.jpg --cache=local:rw,remote: && turbo build --filter=admin --graph=graphs/admin.jpg --cache=local:rw,remote:",
    "lint": "turbo lint --cache=local:rw,remote:",
    "lint:fix": "turbo lint:fix --cache=local:rw,remote:",
    "prep": "turbo prep --cache=local:rw",
    "seed": "turbo seed",
    "start": "turbo start",
    "studio": "turbo studio",
    "test": "turbo test --cache=local:rw,remote:"
  },
  "devDependencies": {
    "@manypkg/cli": "0.23.0",
    "@repo/configs": "workspace:*",
    "rimraf": "6.0.1",
    "turbo": "2.4.1",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.6.2",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "argon2",
      "esbuild",
      "sharp"
    ]
  }
}
